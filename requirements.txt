# Core dependencies for Scenic + MetaDrive scenario generation
# Python 3.8+ required
#
# NOTE — three LOCAL modules must also be present in the repo alongside
# train_hwyenv_stopandgo_lstmppo_single.py (they cannot be pip-installed):
#   • HighwayEnv/          custom fork of highway-env with realistic.py
#                          (provides RealisticVehicle, IntersectionState)
#   • train_callback.py    TrainStatsCallback used during PPO training
#   • reward_machine/      RewardMachine package for the RM-PPO method

gymnasium
torch
tensorboard
imageio
numpngw
control
gym
tqdm
celluloid
stable_baselines3[extra]
sb3-contrib              # RecurrentPPO for LSTM-PPO baseline
texttest
highway-env
z3-solver
numpy
matplotlib
opencv-python
scenic
pillow
mss

# Scenic add.
antlr4-python3-runtime ~= 4.11
attrs >= 19.3.0
dotmap ~= 1.3
mapbox_earcut >= 0.12.10
manifold3d >= 2.5.1
networkx >= 2.6
pegen >= 0.3.0
pygame >= 2.1.3.dev8, <3; python_version >= "3.11"
pyglet >= 1.5, <= 1.5.26
python-fcl >= 0.7
Rtree ~= 1.0
rv-ltl ~= 0.1
scikit-image ~= 0.21
scipy ~= 1.7
shapely ~= 2.0
trimesh >=4.6.0, <5


# MetaDrive add.
requests
yapf
tqdm
progressbar
lxml
psutil
filelock
Pygments
mediapy
panda3d>=1.10.14
panda3d-gltf>=0.13, <1.0
gymnasium

# Additional MetaDrive optional dependencies
sumolib >= 1.21.0
traci
