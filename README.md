# Traffic-Rule-Compliant-Autonomous-Driving-Using-RM

Install using: pip install -r requirements.txt

To run: venv/bin/python train_hwyenv_stopandgo_lstmppo_single.py. Currently the configuration for each train/run is done manually at Lines 716-719 with this quick initial update. Will definitely formalize it in a better form soon.